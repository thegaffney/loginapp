# Prerequisites

You'll need an existing PostgreSQL database for this to work

Put your DB connection info in a .env file in the root, and make sure the variables are named the same as the variables needed under /src/lib/common/db_postgresql.ts

The basic schema for the database is under the schema folder

## Install Dependencies


```bash
npx install
```

## Running The App


```bash
npm run dev

```
